# Ensure shell scripts always use LF line endings regardless of the OS
# where the repository is checked out. CRLF in a shebang line causes
# "/usr/bin/env: 'bash\r': No such file or directory" on Linux.
*.sh text eol=lf
